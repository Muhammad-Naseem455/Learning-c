//STACK is linear data stucture that allows insertion and deletion only at one end
//LIFO PRINCIPLE:
//The most recently added element is removed first
//Basic stack operations:
//1)push():inserts element onto top of the stack
//2)pop():removes and returns top element of the stack(works with index)example:pop(0)
//3)peek():returns top element but doesnot remove it.
//Secondary stack operations;
//top():returns lasst inserted element without removing it
//size():return the size or number of elements in the stack.
//isempty():returns true if stock is empty, else returns false.A stack is empty when top=-1
//isfull():checks whether stack reached max capacity